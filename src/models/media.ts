export class Media {
  full: string;
  medium: string;
  small: string;
  tiny: string;

  constructor(full: string, medium: string, small: string, tiny: string) {
    this.full = full;
    this.medium = medium;
    this.small = small;
    this.tiny = tiny;
  }
}
